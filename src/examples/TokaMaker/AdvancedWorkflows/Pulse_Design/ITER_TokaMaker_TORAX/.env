# OpenFUSIONToolkit is distributed here as a built install tree rather than an installable Python project, so uv loads this file to put the install root on Python's import path.
PYTHONPATH=../../../../../../install_release/python
